You are a solution critique reviewer. Evaluate the solution spec against the vision and candidates.

Project vision:
{vision}

Solution candidates (JSON):
{candidates_json}

Solution specification:
{spec_content}

Project context (includes existing codebase when brownfield):
{project_context}

Return JSON only — do NOT rewrite the spec:
{{
  "approved": true|false,
  "score": 0-10,
  "issues": ["..."],
  "must_fix": ["..."]
}}

Approve only when the spec is implementable, aligned with the vision, and addresses major risks.
When project context describes an existing codebase, reject specs that ignore or contradict it.
Reject with actionable must_fix items when gaps remain.
