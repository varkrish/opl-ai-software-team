YOUR JOB: Review and complete ALL frontend/UI files defined in tech_stack.md.
The development phase already created initial files — your job is to ensure they are
COMPLETE, CORRECT, and match the project's tech stack exactly.

Requirements: {requirements}
Design Specs: {design_specs}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 1: READ TECH STACK TO DETERMINE FRAMEWORK AND FILE STRUCTURE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Call file_reader(file_path='tech_stack.md')

From tech_stack.md, identify:
- The frontend framework (Angular, React, Vue, React Native, Svelte, etc.)
- The complete list of frontend files to create
- Directory structure and naming conventions
- Dependencies required

CRITICAL: Use ONLY the framework specified in tech_stack.md.
Do NOT assume React Native or any other framework — read the file first!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 2: LIST AND READ EXISTING FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Call file_lister(directory='.') to see what exists at the root.
Then list relevant subdirectories (frontend/, src/, etc.).

For each existing frontend file, call file_reader to check if it is:
- Complete (has all imports, exports, and logic)
- Properly structured for the framework
- Non-empty and non-truncated

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 3: FIX INCOMPLETE OR MISSING FILES
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
For each file in tech_stack.md's file structure:

A. If the file EXISTS but is INCOMPLETE or TRUNCATED:
   - Rewrite it with FULL, working content using file_writer
   - Ensure all imports, class definitions, and logic are complete

B. If the file is MISSING:
   - Create it with file_writer using the CORRECT path from tech_stack.md

C. If the file EXISTS and is COMPLETE:
   - Skip it (do not rewrite working files)

IMPORTANT — Framework-specific guidelines (use ONLY if matching tech_stack.md):
- Angular: Files go under frontend/src/app/. Use TypeScript (.ts). Include modules,
  components, services. Ensure HttpClientModule is imported in app.module.ts if
  the app makes HTTP requests.
- React: Files go under src/. Use JSX/TSX. Include proper imports from 'react'.
- Vue: Files go under src/. Use .vue single-file components.
- React Native: Files go under src/. Include React Native imports. Create
  metro.config.js and babel.config.js at root.

DIRECTORY STRUCTURE: Use forward-slash separated paths (e.g., com/example/todo/),
never dot-separated (e.g., NOT com.example.todo/).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
STEP 4: VERIFY COMPLETENESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. List all directories again to confirm files are in place
2. Read each file you created/modified to verify it is complete
3. Count: tech_stack.md frontend files vs files on disk — they MUST match

Your final answer MUST list ALL frontend files with status:
"✅ Verified <path> — complete"  or  "✅ Created <path>"  or  "✅ Fixed <path>"

DO NOT give your final answer until ALL frontend files from tech_stack.md are verified!
